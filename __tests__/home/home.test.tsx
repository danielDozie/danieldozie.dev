import { expect, test, jest } from '@jest/globals';

describe('Home components tests for each section of the page', () => {
    describe('Home Hero section', () => {
        test('Should contain an image and a text description', () => {
            expect('Will write my tests here').toBe('Will write my tests here');
        });
    })
})